# Build services/node-agent locally, inspect its immutable image ID, and set it here.
# A published repository@sha256:digest reference is also accepted.
NODE_AGENT_IMAGE=sha256:replace-with-64-hex-image-id

# Use: stat -c '%g' /var/run/docker.sock
DOCKER_GID=999
DOCKER_API_VERSION=1.44

# The externally reachable IPv4 address or DNS name used in generated VPN configs.
SERVER_PUBLIC_HOST=vpn.example.com
SERVER_ID=00000000-0000-4000-8000-000000000000
SERVER_NAME=Amnezia node
SERVER_REGION=region-1
SERVER_WEIGHT=100
SERVER_MAX_PEERS=500
