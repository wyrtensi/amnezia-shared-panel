{
  "name": "@amnezia/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "amnezia-panel": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/main.ts",
    "lint": "eslint src",
    "start": "node dist/main.js",
    "pretest": "pnpm --filter @amnezia/contracts build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@amnezia/contracts": "workspace:*",
    "tsx": "4.21.0"
  }
}
