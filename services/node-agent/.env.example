# Адрес и порт Fastify
FASTIFY_ROUTES = localhost:4001

# Ключ доступа к API, используется в заголовке x-api-key
FASTIFY_API_KEY = change-me

# Разрешенные browser origin через запятую; пустое значение отключает CORS
# Пример: https://panel.example.com,http://localhost:3000
CORS_ORIGINS =

# Включенные протоколы
PROTOCOLS_ENABLED = amneziawg,amneziawg2,xray

# Уникальный идентификатор сервера
SERVER_ID = 4b90100e-f40f-4ef8-a0c0-8c0cdd08556e

# Название сервера, будет отображаться в клиенте
SERVER_NAME = Сервер

# Регион сервера 
SERVER_REGION = Server-1

# Вес сервера для балансировки (целое число; больше - приоритетнее)
# Рекомендуемый диапазон: 1..1000
SERVER_WEIGHT = 100

# Максимально допустимое число клиентов на сервере
SERVER_MAX_PEERS = 500

# Внешний хост/домен для Endpoint
SERVER_PUBLIC_HOST = 0.0.0.0

# Версия готового GHCR-образа для docker-compose.ghcr.yml
AMNEZIA_API_VERSION = latest

# Обновление агента из панели. Пусто - функция выключена: POST /server/update
# отвечает 501, потому что на хосте некому выполнить замену. Включается
# скриптом infra/node/scripts/install-agent-updater.sh.
# Принимается только digest в этом репозитории: тег изменяемый, и установлено
# было бы не то, что подтвердил администратор.
NODE_AGENT_UPDATE_REPO =
# Каталог спула внутри контейнера (том смонтирован сюда из хоста).
NODE_AGENT_UPDATE_SPOOL = /var/lib/node-agent/update
